import Box from '@mui/material/Box';
import { styled } from '@mui/system';


const StyledMushroomInfo = styled(Box)(({ theme }) => ({
    width: '600px',

    '.info' : {
        paddingLeft: '10px !important'
    }
}));

export default StyledMushroomInfo;
